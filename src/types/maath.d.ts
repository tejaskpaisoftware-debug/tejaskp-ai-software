declare module 'maath/random/dist/maath-random.esm' {
    export function inSphere(buffer: Float32Array | Float64Array, options?: { radius?: number; center?: number[] }): Float32Array;
    // Add other exports as needed
}
